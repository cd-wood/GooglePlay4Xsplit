<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ViewModel="clr-namespace:GooglePlay4XSplit.ViewModel"
        xmlns:local="clr-namespace:GooglePlay4XSplit.View" x:Class="GooglePlay4XSplit.View.MainWindowView"
        Title="MainWindowView" Height="350" Width="525">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </Window.Resources>
    <Window.DataContext>
        <ViewModel:MainWindowViewModel/>
    </Window.DataContext>
    <Grid>
        <ContentControl HorizontalAlignment="Center" Margin="0,0,0,0" VerticalAlignment="Center" Content="{Binding LoginControlViewModel}" Visibility="{Binding IsNotLoggedIn, Converter={StaticResource BooleanToVisibilityConverter}, Mode=OneWay}"/>
    </Grid>
</Window>
